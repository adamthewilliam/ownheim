{
  "name": "@ownheim/example-trpc-api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/router.ts",
    "generate": "bunx ownheim generate"
  },
  "dependencies": {
    "@ownheim/core": "workspace:*",
    "@ownheim/trpc": "workspace:*",
    "@trpc/server": "^11.8.0",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@ownheim/cli": "workspace:*"
  }
}
